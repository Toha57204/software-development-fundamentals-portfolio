# Lecture 02 - Data Types, Variables, Operators and Expressions

**Course:** ITAP1001 - Software Development Fundamentals  
**Lesson:** 2

## Topics Covered

- Data Types
- Variables
- Literals
- Expressions
- Mathematical Operators
- Comparison Operators
- Logical Operators
- Operator Precedence
- Type Conversion
- Python Math Library
- Built-in Methods and Libraries

## What I Learned

In this lesson, I learned how Python stores and interprets different types of data.

I practised working with:

- Integers
- Floating-point numbers
- Strings
- Boolean values
- Variables
- Mathematical expressions
- Comparison expressions
- Logical expressions

Python determines a variable's data type from the value assigned to it.

Example:

```python
name = "xyz"
weight = 62.8
height = 168
```

Here:

- `name` is a string
- `weight` is a floating-point value
- `height` is an integer

I also learned the main mathematical operators:

```text
+  -  *  /  %
```

Comparison operators:

```text
<  <=  ==  >  >=  !=
```

Logical operators:

```python
and
or
not
```

The lesson also introduced Python's `math` library.

Example:

```python
import math

root = math.sqrt(25)
power = math.pow(2, 3)
```

## Files

- `lecture-notes.md` - Detailed notes from Lecture 2
- `practical-exercise-02.md` - Practice based on Lesson 2 concepts

## Quick Reference

| Concept | Example |
|---|---|
| Integer | `10` |
| Float | `10.5` |
| String | `"VIT"` |
| Boolean | `True` |
| Variable | `score = 75` |
| Integer conversion | `int(68.2)` |
| Addition | `x + y` |
| Subtraction | `x - y` |
| Multiplication | `x * y` |
| Division | `x / y` |
| Remainder | `x % y` |
| Equal to | `x == y` |
| Greater than | `x > y` |
| Logical AND | `and` |
| Logical OR | `or` |
| Logical NOT | `not` |
| Math library | `import math` |

## Learning Progress

✅ Data Types  
✅ Variables  
✅ Literals  
✅ Expressions  
✅ Mathematical Operators  
✅ Comparison Operators  
✅ Logical Operators  
✅ Operator Precedence  
✅ Type Conversion  
✅ Python Math Library
